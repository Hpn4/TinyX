---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: srvc-post-ingress
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /posts
            pathType: Prefix
            backend:
              service:
                name: srvc-post-service
                port:
                  number: 8080
