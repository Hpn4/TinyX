---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: srvc-search-ingress
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /search
            pathType: Prefix
            backend:
              service:
                name: srvc-search-service
                port:
                  number: 8080
