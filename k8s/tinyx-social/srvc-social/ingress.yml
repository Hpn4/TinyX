---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: srvc-social-ingress
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /social
            pathType: Prefix
            backend:
              service:
                name: srvc-social-service
                port:
                  number: 8080
