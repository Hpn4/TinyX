---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: srvc-user-timeline-ingress
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /timeline
            pathType: Prefix
            backend:
              service:
                name: srvc-user-timeline-service
                port:
                  number: 8080
